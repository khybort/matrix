{
  "name": "matrix",
  "version": "0.0.1",
  "private": true,
  "description": "Self-learning context-graph AI research engine for multi-market intelligence",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
