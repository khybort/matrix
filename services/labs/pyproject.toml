[project]
name = "matrix-labs"
version = "0.0.1"
description = "Evolutionary algorithm-search lab — tries new strategy compositions."
requires-python = ">=3.13"
dependencies = [
    "matrix-shared",
    "matrix-agent",
    "httpx>=0.28.0",
    "orjson>=3.10.0",
    "loguru>=0.7.3",
]

[tool.uv.sources]
matrix-shared = { path = "../../packages/python-shared", editable = true }
matrix-agent = { path = "../agent", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/labs"]

[project.scripts]
matrix-labs = "labs.main:main"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "RUF"]
