# packages/ts-shared

Shared TypeScript types + utilities for the web app.

**Owner:** Machine B (Output)

Includes:
- Generated types from Postgres schema (via `drizzle-kit` or `pg-typegen` — TBD)
- Shared zod schemas for API request/response shapes
- Utility helpers consumed across `apps/`
